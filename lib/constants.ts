
/**
 * Application-wide constants
 * 
 * Centralized constants for consistent messaging and configuration across the UI.
 */

/**
 * Default availability message shown when book.availabilityMessage is not provided.
 * 
 * This message is intentionally neutral and does not make inventory promises.
 * It should be used consistently across all customer-facing UI components.
 */
export const DEFAULT_AVAILABILITY_MESSAGE = 'Available for order';

